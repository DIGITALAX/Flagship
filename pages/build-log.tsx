import { NextPage } from "next";

const BuildLog: NextPage = (): JSX.Element => {
  return (
    <div className="min-w-screen min-h-full h-full flex flex-col bg-mainBg justify-start items-center pb-28 gap-32">
      <div className="relative w-full h-screen flex flex-col items-center justify-start gap-3"></div>
    </div>
  );
};

export default BuildLog;
